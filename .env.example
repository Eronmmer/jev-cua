# Prefer macOS Keychain over a plaintext environment file.
TYPESAFE_API_KEY=
# This changes only content-free trace storage. The at-most-once run-key ledger and
# desktop lease always use a fixed per-user location.
JEV_CUA_STATE_DIR=/Users/you/.local/state/jev-cua
